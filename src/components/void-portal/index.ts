/**
 * Void Portal — the cosmic background of the home page.
 *
 * Two presentational SVG components plus the design-token object they
 * (and the home-route's embedded CSS) share. Self-contained: no app state,
 * no router awareness.
 */
export { Stars } from "./stars";
export { VoidPortal } from "./void-portal";
export { vp } from "./tokens";
